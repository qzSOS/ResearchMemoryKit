# Workflow

## Memory Completion Gate

An experiment is not complete until:

1. registry status is updated;
2. metrics are written if available;
3. conclusion or failure is recorded;
4. pitfalls are updated if a bug was found;
5. session activity is appended;
6. Current State is replaced if the active state changed.
